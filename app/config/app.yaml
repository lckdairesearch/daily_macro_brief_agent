# app/config/app.yaml — runtime settings
# Secrets must come from environment variables, not here.

run:
  timezone: "Asia/Hong_Kong"
  data_cutoff_minutes_before_send: 30  # approx 06:45 HKT

output:
  dir: "outputs"

logging:
  level: "INFO"
